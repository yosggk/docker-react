language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t yo1sggk1/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true yo1sggk1/docker-react npm run test -- --coverage